# Багийн ажлын дүрэм

Энэ файл нь CI улаан болох, эсвэл ажиллаж байсан функц чимээгүй алга болохоос
сэргийлэх цөөн дүрмийг агуулна. Урт биш — бүгдийг нь уншаарай.

## Эхлэх

```bash
git clone https://github.com/bileguudei/camera-quest.git
cd camera-quest
nvm use
npm ci
npm run dev
```

Энэ бүхэн. `.env.local` байхгүй бол эхний `npm run dev` дээр өөрөө үүснэ:
тоглоом бүхэлдээ таны машин дээр ажиллана — Supabase, Modal, ямар ч данс
шаардахгүй. Дэлгэц, тоглоомын урсгал дээр ажиллахад энэ хангалттай.

Онлайн өрөө, жинхэнэ AI таниалтыг турших хэрэгтэй болбол project owner-оос
дөрвөн `NEXT_PUBLIC_*` утга аваад `.env.local` дотроо бөглөнө. Зөвхөн
`NEXT_PUBLIC_` эхэлсэн утгыг л дамжуулна — service role key хэзээ ч биш.

## Node хувилбар

CI **Node 22** дээр ажилладаг. Та ч мөн адил байх ёстой:

```bash
nvm use
```

`.nvmrc` файл 22-ыг заана. Өөр хувилбар дээр `npm install` ажиллуулбал lock файл
өөрчлөгдөж, CI дээр `npm ci` унана (доороос харна уу).

## package-lock.json

**Хамаарал (dependency) нэмээгүй бол lock файлаа commit-даа огт бүү оруул.**

`npm install` нь таны үйлдлийн системд хэрэггүй optional пакетуудыг хасдаг. Тэр
lock-ыг commit хийвэл Linux дээрх CI тэдгээрийг олж чадахгүй болж `npm ci` унана.
Яг ийм шалтгаанаар PR #10 дээр гурван job зэрэг унасан.

Алдаа гарвал:

```bash
# Хамаарал өөрчлөөгүй бол — lock-оо буцаа:
git checkout origin/main -- package-lock.json

# Үнэхээр шинэ пакет нэмсэн бол — Node 22 дээр дахин үүсгэ:
nvm use && npm install && git add package-lock.json
```

Push хийхээсээ өмнө өөрөө шалгаж болно:

```bash
npm run check:lockfile
```

CI мөн үүнийг `npm ci`-аас өмнө ажиллуулж, юу хийхийг шууд хэлж өгнө.

## Push хийхээс өмнө

```bash
npm run verify
```

Энэ нь lint, typecheck, тест, contract, build-ыг бүгдийг ажиллуулна. Ногоон
байвал CI ч ногоон байх магадлал өндөр.

Хэрэв та `supabase/migrations/` дотор ямар нэг зүйл өөрчилсөн бол:

```bash
supabase db reset && npm run generate:database
git add src/generated/database.types.ts
```

## Дизайн шинэчлэхэд

Дэлгэц дахин зурахдаа **байгаа товч, гарцыг санамсаргүй бүү устга**. Landing
дээрх «Найзтайгаа онлайн» товч нэг удаа ингэж алга болж, онлайн горим руу орох
цорын ганц гарц хаагдсан — бусад бүх тест ногоон хэвээр байсан.

Одоо `Landing.test.tsx` энэ хоёр гарцыг хоёуланг нь шалгадаг. Ижил төрлийн
чухал гарц нэмэх бүрдээ ийм тест бичээрэй.

## Migration

Аль хэдийн push хийгдсэн migration файлыг **хэзээ ч бүү засварла**. Шинэ файл
нэмж залруул — staging болон бусад хүний локал DB тэр файлыг аль хэдийн
ажиллуулсан байдаг.

Мөн зөвхөн staging дээр SQL ажиллуулаад migration файл нэмэхээ мартвал өөрчлөлт
чинь дараагийн `db reset` дээр алга болно.
